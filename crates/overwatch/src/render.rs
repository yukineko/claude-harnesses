/// Status rendering and output formatting.
use anyhow::Result;

pub fn status(json: bool) -> Result<()> {
    unimplemented!("render::status")
}

pub fn sessions(json: bool) -> Result<()> {
    unimplemented!("render::sessions")
}
