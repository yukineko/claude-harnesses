# harness 仕様↔実装 整合監査 2026-07-13

## スコープ (全体)
- baseline: `HEAD~20`
- canon commit (HEAD): `db79a7991f554670fb217a819fb6abfdbc6cc50a`
- 変更ファイル数: 164
- shard: condukt, specguard, harness-core, invariants
- 各 shard は独立した read-only エージェント (fresh context) で監査し、ここに統合した。

---

## shard: condukt

# harness 仕様↔実装 整合監査 2026-07-13

## スコープ
- baseline: HEAD~20
- 監査した領域: condukt (実装: circuit.rs, claim.rs, config.rs, escalate.rs, lessons.rs, main.rs, precedent.rs, state.rs / canon: crates/condukt/README.md, crates/condukt/skills/condukt/SKILL.md)
- skip した領域: condukt 以外の全領域 (他 shard が担当), 不変条件全般 (本 shard では扱わない指示のため)
- 不変条件: (本 shard では対象外)

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| reconcile のクロスラン重複ガード: 事前検知・非変更・JSON出力・exit 2 | crates/condukt/README.md:53 | "before that auto-promotion, it scans sibling runs for any hashkey this run completed (`done`/`verified`) that another `run_id` *also* completed *after* this run's `claimed_at`; on a hit it mutates nothing, prints `{"duplicate_completion":[{hashkey,runs:[run_id...]}]}`, and **exits 2**" | crates/condukt/src/main.rs:3336-3348, crates/condukt/src/state.rs:637-691 | 整合 | - | no | 対応不要 |
| 重複無し経路は従来どおり auto-verify・exit 0 | crates/condukt/README.md:53 | "The no-duplicate path is unchanged (auto-verify, exit 0)." | crates/condukt/src/main.rs:3349-3377 (dups が空なら reconcile_run へフォールスルー、exit code 変更なし) | 整合 | - | no | 対応不要 |
| "completed" の定義 = done/verified、claimed_at 欠落は 0 にフェイルソフト | crates/condukt/README.md:53 (`done`/`verified`, `claimed_at`の言及) | 上に同じ引用を参照 | crates/condukt/src/state.rs:620-625 ("A task counts as \"completed\"... when it reached... `done` or `verified`"), 661 ("Missing claimed_at fails soft to 0") | 整合 | - | no | 対応不要 |
| SKILL.md Phase 7 の `condukt state reconcile` 手順が exit 2 (escalate) 経路に言及していない | crates/condukt/skills/condukt/SKILL.md:981-985 | "gate FAIL の場合、**まず reconcile を試みる**（branch がマージ済みのタスクを自動 verified に昇格）:\n  ```\n  condukt state reconcile --run $RID\n  condukt state gate --run $RID    # 再チェック\n  ```" | crates/condukt/src/main.rs:3334-3348 (reconcile が duplicate 検知時に exit 2 で終了しうる) | 仕様未記載 | - | yes | SKILL.md Phase 7 に「reconcile が exit 2 の場合は `duplicate_completion` JSON を読み、人間に escalate する（gate 再チェックに進まない）」旨の分岐を追記 |

### D2 仕様品質 (spec-quality)
| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| 沈黙 | crates/condukt/README.md:44-53 (コマンド一覧表), crates/condukt/src/main.rs (`Command::Adversarial { action }` at main.rs:1735, `AdversarialAction::Plan/Vote`) | README:44 に "`condukt consensus plan/vote`" は記載されているが、対になる "`condukt adversarial plan/vote`" コマンド行はコマンド表に一切存在しない (grep で0件) | yes | README のコマンド表に `condukt adversarial plan/vote` の行を追加 (consensus の隣。plan/vote の意味論・exit-code契約・GATE crate強制条件を記述) |
| 沈黙 | crates/condukt/README.md:137-175 (`config.toml` 例ブロック), crates/condukt/src/config.rs:52-61 (`adversarial_enabled`/`adversarial_size`/`adversarial_min_voters`/`adversarial_block_ratio` フィールドと doc comment) | README:157-160 に `[consensus]` セクションの例はあるが `[adversarial]` セクションの例は無い (grep で0件)。config.rs のコメントには "Overridable via `CONDUKT_ADVERSARIAL`" と明記 | yes | README の config.toml 例ブロックに consensus セクションに準じた `[adversarial]` (enabled/size/min_voters/block_ratio) のコメント付き例を追加 |
| 沈黙 | crates/condukt/README.md:186-197 (環境変数表) | 表には `CONDUKT_CONSENSUS`/`CONDUKT_AUTONOMOUS`/`CONDUKT_SINGLE_WORKTREE`/`CONDUKT_WORKER_SANDBOX`等はあるが `CONDUKT_ADVERSARIAL` 行が無い。一方 crates/condukt/src/config.rs:339-343 は `std::env::var("CONDUKT_ADVERSARIAL")` を実装し main.rs コメントも "the global switch comes from config.toml `[adversarial] enabled` / `CONDUKT_ADVERSARIAL`" と明記 | yes | 環境変数表に `CONDUKT_ADVERSARIAL` の行を追加 (`CONDUKT_CONSENSUS` と同形式) |

## 修正候補 (needs_user=yes / 承認待ち)
- [D1] SKILL.md Phase 7 の reconcile 手順に exit 2 (cross-run duplicate escalate) 分岐が無い / 実装箇所: crates/condukt/src/main.rs:3334-3348 / 正典: crates/condukt/skills/condukt/SKILL.md:981-985 / 分類: 判別不能 (doc 更新で足りるか、独立した安全対策が必要かは人間判断) / 推奨対応: SKILL.md Phase 7 に reconcile の exit 2 時の対応 (duplicate_completion JSON を読み human へ escalate) を追記
- [D2] 沈黙 / 該当: crates/condukt/README.md (コマンド表・config.toml例・環境変数表いずれも `adversarial`/`CONDUKT_ADVERSARIAL` を欠く) / 推奨対応: README に `condukt adversarial plan/vote` コマンド説明、`[adversarial]` config 例、`CONDUKT_ADVERSARIAL` 環境変数行を追加 (README:44-53 コマンド表, 137-175 config例, 186-197 環境変数表の3箇所すべて)

---

## shard: specguard

# harness 仕様↔実装 整合監査 2026-07-13

## スコープ
- baseline: HEAD~20
- 監査した領域: specguard (`crates/specguard/src/decision.rs`, `crates/specguard/src/scope.rs`)
- skip した領域: 他の全領域・全不変条件（別 shard が並列監査。本 shard は specguard の実装差分 2 件のみに集中）
- 不変条件: (本 shard では対象外)

## findings

### D1 実装↔正典 drift

| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| D3 decisions dir は `""` で無効化、in-repo dir か Obsidian vault path を指す（欠落 dir の扱いは self-evident に「無効」） | `crates/specguard/INSTALL.md:206-207` | "`[decisions] dir` can point at an in-repo directory or an Obsidian vault path (`""` disables it)." | `crates/specguard/src/decision.rs` `list_files()` (dir 欠落 → `Ok(vec![])`) | 整合 | - | no | なし |
| D3 「decision が canon にまだ合致するか (freshness) / driver・review_when がまだ成り立つか (staleness)」を毎回チェックする、という audit の前提（対象ファイル一覧が取得できて初めて成立する） | `crates/specguard/INSTALL.md:202-205` | "Subsequent `specguard run`s then perform the **D3 audit**, checking for each decision (a) whether the canon it points at still matches (freshness), and (b) whether the driver/review_when still hold (staleness ...)." | `crates/specguard/src/decision.rs:list_files`（dir が存在するが読めない場合・per-entry エラーの場合に `Err` を返し fail-closed。旧実装は空リストを返し D3 shard を静かにスキップしていた= false-GREEN） | 整合 | - | no | 変更後は「decision ごとに必ずチェックする」という正典の前提（読めた分だけを都合よく"decision なし"扱いしない）に沿う。旧実装が正典に対する drift（B寄り）だったが、本 shard の diff は既にその是正コミット（f0933c7, CA-specguard-001）であり現状のコードは整合。 |
| `[decisions] dir` が指すディレクトリが「存在するが読めない」場合の挙動（fail-open か fail-closed か、exit code 何番になるか） | `crates/specguard/README.md:287-293`（Exit codes 表）／`crates/specguard/INSTALL.md:187-207`（D3 節） | Exit codes 表: "\| 2 \| config / usage error \|" のみ列挙。D3 節にはこのケースの記述なし。 | `crates/specguard/src/scope.rs:308` (`crate::decision::list_files(...)?` で `Err` を `main.rs` の `run()` へ伝播し、最終的に `EXIT_USAGE`(=2) にマップされる) | 仕様未記載 | - | no | 正典の exit code 2 は「config/usage error」と定義されているが、decisions dir の I/O エラー (`std::io::Error`, permission denied 等) はその定義に文言上は該当しない。実質的な fail-closed の意図は正しいが、doc にこのケースの exit code を明記すると良い（silence 是正、緊急性は低い） |

### D2 仕様品質 (spec-quality)

| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| 沈黙 | `crates/specguard/INSTALL.md:187-207`（5.5 節 D3）/ `crates/specguard/README.md:268,281-283`（D3 概要） | INSTALL.md: "`[decisions] dir` can point at an in-repo directory or an Obsidian vault path (`""` disables it)."／README.md: "`[decisions]` … enable the decision-record (ADR) freshness/staleness check (D3)" | no | 「decisions dir が存在するが読めない（permission denied 等）場合は fail-closed してエラー終了する」という挙動（`crates/specguard/src/decision.rs` の `list_files` が体現）を正典に一文追記すると、今後同種の fail-open drift を防ぎやすい。ただし現状ユーザー向け機能仕様としては軽微で、doc化必須とまでは言えないため needs_user=no とした |

## 修正候補 (needs_user=yes / 承認待ち)
- なし

---

## shard: harness-core

# harness 仕様↔実装 整合監査 2026-07-13

## スコープ
- baseline: HEAD~20
- 監査した領域: harness-core (`crates/harness-core/src/lessons.rs` の変更、正典ポインタ `docs/OVERVIEW.md`)
- skip した領域: 他の全領域・不変条件 (別 shard が担当。本 shard はスコープ外)
- 不変条件: (本 shard では扱わない)

## findings
### D1 実装↔正典 drift

| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| harness-core の公開関数はドキュメントのモジュール表に記載する | `docs/OVERVIEW.md:34-46` | `\| モジュール \| 役割 \|\n\|---\|---\|\n\| \`hook.rs\` \| \`HookInput\` パース、\`run_hook\` 不変ラッパー \|\n\| \`store.rs\` \|...` (表に `lessons.rs` の記載なし) | `crates/harness-core/src/lessons.rs:242-250` (`pub fn text_similarity`) | 仕様未記載 | — | yes | `docs/OVERVIEW.md` の共通基盤モジュール表に `lessons.rs`（他クレートから再利用される公開 API を持つモジュール）を追加するか、少なくとも本モジュール表が「網羅ではなく代表例」である旨を明記する |
| `text_similarity` のトークナイズ仕様（小文字化・英数字・3文字以上・stopword 除去）が実装と一致するか | `crates/harness-core/src/lessons.rs:214-231` (docstring自身が正典代わり。OVERVIEW.md に該当記述なし) | `"Normalise free text into a token set: lowercased, alphanumeric, min length 3, stopwords dropped."` / 実装: `if t.len() >= 3 && !STOP.contains(&t)` | `crates/harness-core/src/lessons.rs:223-230` | 整合 | — | no | 対応不要（docstring と実装が一致） |

### D2 仕様品質 (spec-quality)

| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| 沈黙 | `docs/OVERVIEW.md:34-46`（共通基盤 harness-core モジュール表） | `\| モジュール \| 役割 \|\n\|---\|---\|\n\| \`hook.rs\` \|...\| \`inject.rs\` \| コンテキスト注入プラグイン（playbook / runbook）の共有基盤 \|` (表は7モジュールのみ列挙。実際は `crates/harness-core/src/` に24モジュールが存在し `lessons.rs` はそのいずれにも該当しない) | yes | 表を「代表的なモジュールのみ」と明記するか、少なくとも他クレートから直接呼ばれる公開 API を持つモジュール（`lessons`, `retrieval`, `scorer` 等）は追記する。今回の `text_similarity`（overwatch から呼ばれるクロスクレート API）のような「他クレート再利用の契約」が生まれた時点で追従できていないのが本質的な沈黙 |

## 修正候補 (needs_user=yes / 承認待ち)
- [D1] harness-core モジュール表への `lessons.rs` 記載欠落 / 実装箇所: `crates/harness-core/src/lessons.rs:242-250` / 正典: `docs/OVERVIEW.md:34-46` / 分類: 判別不能（表が意図的に代表例のみを示す設計か、単なる更新漏れかは doc からは判別できない） / 推奨対応: doc 保守者に表の意図（網羅 or 代表列挙）を確認し、網羅の意図であれば `lessons.rs`（および他の未掲載モジュール）を追記
- [D2] 沈黙 / 該当: `docs/OVERVIEW.md:34-46` / 推奨対応: モジュール表の網羅性ポリシーを明文化するか、クロスクレート公開 API（今回の `text_similarity` など）を持つモジュールは追記するルールを追加

---

## shard: invariants

# harness 仕様↔実装 整合監査 2026-07-13

## スコープ
- baseline: HEAD~20
- 監査した領域: なし (D1 領域監査は対象外 — 本 shard は不変条件のみ)
- skip した領域: 全 D1 領域 (別 shard で実施)
- 不変条件: hook never panics (harness-core::hook::run_hook が常に exit 0 で返す)

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| hook never panics | docs/OVERVIEW.md:26-27 (「2. フック不変条件（never break a turn）」) | "`harness-core::hook::run_hook` がパニックを捕捉して常に exit 0 で返す。どのプラグインのバグも Claude Code のターンを壊さない、という不変条件がコアライブラリに焼き込まれている。" | crates/harness-core/src/hook.rs:218-221 (`pub fn run_hook<F: FnOnce() + UnwindSafe>(f: F) -> ! { let _ = catch_and_log("hook", f); std::process::exit(0); }`) | 整合 | - | no | 対応不要。`catch_and_log`(190-213行、`std::panic::catch_unwind`でパニックを捕捉しstderrへログ)を呼んだ後、結果によらず無条件で`std::process::exit(0)`し、`!`(never)型で返る。テスト`catch_and_log_returns_false_on_panic_and_true_on_ok`(463-469行)がパニック捕捉を検証済み。|

### D2 仕様品質 (spec-quality)
| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| (findings なし) | - | - | - | - |

D2 について: 同一不変条件の他 doc (docs/AGENTIC-CODING-GUIDE.md, docs/GLOSSARY.md, docs/USAGE.md) 上の "exit 0" / "never break a turn" 言及を確認したが、いずれも別文脈 (git hooks の fail-soft, tdd バイナリ不在時の fail-soft, condukt gate 完了条件) であり、`run_hook` 不変条件そのものの実体コピーではない。重複・沈黙・矛盾は検出されず。

## 修正候補 (needs_user=yes / 承認待ち)
- なし
