# Plugin activation scopes

Every plugin in this monorepo is classified by **how loud its activation surface
is** — i.e. how often its code runs relative to the model's turns. This keeps the
always-on hook / injection budget (see
[`crates/ctxrot/docs/adr/0001-cross-harness-injection-budget.md`](../crates/ctxrot/docs/adr/0001-cross-harness-injection-budget.md))
legible: you can see at a glance which plugins pay a per-turn cost and which only
run on demand.

The taxonomy is generated, not hand-maintained. Regenerate the classification
below with:

```sh
cargo run -q -p harness-status -- plugins          # human table
cargo run -q -p harness-status -- plugins --json    # machine-readable + counts
```

## The three scopes

The scanner reads each crate's `hooks/hooks.json` (collecting the top-level event
keys), plus whether it has non-empty `skills/` and `agents/` dirs.

- **Always-on** — registers a hook on a **high-frequency lifecycle event** that
  fires basically every turn/session. The always-on event set is:
  `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`,
  `SubagentStop`. Any one of these makes a plugin always-on. These are the
  plugins whose cost is paid on (nearly) every turn, so they are the ones the
  injection/latency budgets watch.
- **Event-scoped** — registers hooks **only** on lower-frequency / conditional
  events (none of the always-on set): e.g. `PreCompact`, `SessionEnd`,
  `Notification`. They run, but rarely — not on the hot path of every turn.
- **Manual** — registers **no hooks at all**. Reachable only via a skill (slash
  command) or direct CLI. A Manual plugin with a skill is a slash-command; one
  with neither skill nor agent and only a binary (like `harness-status`) is
  CLI-only.

Classification rule (pure, unit-tested in `crates/harness-status/src/plugins.rs`):
if `hook_events` contains any always-on event → **Always-on**; else if
`hook_events` is non-empty → **Event-scoped**; else → **Manual**. `has_skills` /
`has_agents` never change the scope — they only inform the human "trigger"
description.

A crate dir is treated as a plugin iff it has any of `.claude-plugin/`, `hooks/`,
`skills/`, or `agents/`. Plain library crates (`harness-core`, `mutategate`) have
none of these and are **excluded** — they present no plugin surface.

## Current classification

Generated by `cargo run -q -p harness-status -- plugins`:

```
ALWAYS-ON (28)
  autoflow  —  PreCompact, Stop, UserPromptSubmit
  beacon  —  Notification, Stop
  blastguard  —  PreToolUse
  budgetguard  —  Stop
  compass  —  SessionStart, Stop
  condukt  —  PostToolUse, SessionStart, Stop
  context-governor  —  PostToolUse, PreCompact, SessionStart, Stop, SubagentStop, UserPromptSubmit
  ctxrot  —  PostToolUse, PreCompact, PreToolUse, SessionStart, Stop, UserPromptSubmit
  daily  —  SessionStart
  difflog  —  SessionEnd, SessionStart
  donegate  —  Stop
  fetchguard  —  PostToolUse
  fugu-router  —  UserPromptSubmit
  gauge  —  Stop
  harness-status  —  SessionStart
  hypothesis  —  SessionStart
  overwatch  —  SessionStart, Stop
  playbook  —  UserPromptSubmit
  precommit-audit  —  Stop
  propguard  —  Stop
  reviewgate  —  Stop
  runbook  —  UserPromptSubmit
  session-insights  —  PostToolUse, SessionEnd, Stop
  specguard  —  SessionStart
  stuckguard  —  PostToolUse
  taskprog  —  SessionStart, Stop
  tdd  —  Stop

EVENT-SCOPED (1)
  ship  —  SessionEnd

MANUAL (12)
  backlog  —  skill (/backlog)
  benchkit  —  CLI only
  curate  —  CLI only
  daily-report  —  skill (/daily-report)
  deepwiki  —  agent
  evalkit  —  CLI only
  flow  —  skill (/flow)
  replaykit  —  CLI only
  schemaguard  —  CLI only
  scout  —  skill (/scout)
  tracekit  —  CLI only
  trajectoryeval  —  CLI only
```

Counts: **always-on 28 · event-scoped 1 · manual 12 · total 41** (the two
library crates `harness-core` and `mutategate` are excluded — they have no plugin
surface). Regenerated 2026-08-20: `flow` moved to **manual** (its SessionStart
`propose` hook was retired and the plugin ships only the `/flow` skill now) and
`autoflow` lost its own `SessionStart` proposal, leaving `PreCompact`, `Stop`,
`UserPromptSubmit`. This table is illustrative and drifts as plugins are added; regenerate
it with the commands above rather than trusting the numbers here verbatim.

Note `harness-status` is now listed under **Always-on** (it registers a
`SessionStart` hook — see below), not Manual as in earlier revisions of this doc.

## Why harness-status registers one narrow SessionStart hook (and nothing louder)

`harness-status` is still, first and foremost, the **manual HOTL inspection
dashboard**: `harness-status` / `budget` / `sessions` / `progress` / `hooks` /
`inject` / `plugins` / `hooks-health` all remain CLI-only, run on demand. It
deliberately does **not** auto-inject the full dashboard every session — that
would grow the always-on injection/hook budget that ADR 0001 and the
`harness-status inject` / `harness-status hooks` monitors exist to *curb*, and
would make the very tool that measures the budget also spend it.

The one exception is `harness-status session-start` (wired via
`hooks/hooks.json` on `SessionStart`): a hook-binary health check. It reads
`~/.claude/settings.json`, and if any registered hook's `command` points at a
binary that no longer exists on disk (a stale rollout, a pruned cache dir), it
injects a short `additionalContext` warning. When every hook binary is present
— the common case — it prints nothing at all, so it costs zero tokens on a
healthy machine. This crosses the classifier's `SessionStart` line (any
`SessionStart` hook is always-on by definition, regardless of how often it
actually emits output), so `harness-status` now appears under **Always-on** in
the table above — but its actual per-turn cost stays at the silent/no-op end of
that bucket except when something is already broken and worth surfacing.

- `harness-status` — full dashboard (budget + sessions + progress + hook latency + inject)
- `harness-status budget` — today's spend
- `harness-status sessions` — recent sessions
- `harness-status progress` — current progress file
- `harness-status hooks` — Stop-hook latency aggregation (budget monitor)
- `harness-status inject` — UserPromptSubmit injection-size aggregation (budget monitor)
- `harness-status hooks-health` — hook-binary health check (also run on demand)
- `harness-status session-start` — the SessionStart hook entrypoint above
- `harness-status plugins` — this activation-scope classification
