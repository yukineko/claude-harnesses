# tdd config for this repo. `test_cmd` is executed verbatim by `tdd red`/`tdd green`
# (project must be `tdd trust`ed). Scoped to the overwatch crate for the
# continuous-audit finder!=verifier model-diversity enforcement task (t1).
test_cmd = "cargo test -p overwatch"
enabled = true
