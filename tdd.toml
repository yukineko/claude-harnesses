# tdd config for this repo. `test_cmd` is executed verbatim by `tdd red`/`tdd green`
# (project must be `tdd trust`ed). Scoped to the overwatch bridge integration test
# for the finding->backlog bridge task (t3).
test_cmd = "cargo test -p overwatch --test bridge_to_backlog"
enabled = true
